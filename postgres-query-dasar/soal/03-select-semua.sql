-- SOAL 3 — SELECT (AMBIL SEMUA DATA)
--
-- Cerita:
-- INSERT itu ngirim data MASUK. SELECT itu kebalikannya: ambil data
-- KELUAR dari table. Tanda bintang (*) artinya "semua kolom".
--
-- Ketentuan:
-- - table siswa sudah diisi otomatis oleh test (4 baris data)
-- - ambil SEMUA kolom dan SEMUA baris dari table siswa
-- - wajib menggunakan SELECT, *, siswa
-- - dilarang menggunakan WHERE, DROP, DELETE, UPDATE, INSERT, TRUNCATE
--
-- Cara test (jalankan dari folder postgres-query-dasar):
-- npm run test:select-semua

-- TODO: tulis query kamu di sini
SELECT * FROM siswa;

