/**
 * TUGAS 7 — UPDATE STOK BUKU (UPDATE)
 *
 * - require pool dari "../config/database"
 * - async function updateStokBuku(id, stokBaru):
 *   UPDATE buku SET stok = ... WHERE id = ... (WAJIB pakai WHERE!),
 *   pakai RETURNING *, balikin rows[0]
 * - module.exports = updateStokBuku;
 */

// TODO: tulis kode kamu di sini
