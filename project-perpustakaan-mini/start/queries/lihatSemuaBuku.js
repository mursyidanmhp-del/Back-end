/**
 * TUGAS 5 — LIHAT SEMUA BUKU (READ)
 *
 * - require pool dari "../config/database"
 * - async function lihatSemuaBuku(): SELECT semua buku, ORDER BY id
 *   balikin rows-nya
 * - module.exports = lihatSemuaBuku;
 */

// TODO: tulis kode kamu di sini
