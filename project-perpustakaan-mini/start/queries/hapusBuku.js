/**
 * TUGAS 8 — HAPUS BUKU (DELETE)
 *
 * - require pool dari "../config/database"
 * - async function hapusBuku(id):
 *   DELETE FROM buku WHERE id = ... (WAJIB pakai WHERE!), pakai
 *   RETURNING *, balikin rows[0]
 * - module.exports = hapusBuku;
 */

// TODO: tulis kode kamu di sini
