/**
 * TUGAS 4 — TAMBAH BUKU (CREATE)
 *
 * - require pool dari "../config/database"
 * - async function tambahBuku(judul, penulis, tahunTerbit, stok):
 *   INSERT ke tabel buku, pakai RETURNING *, balikin rows[0]
 * - module.exports = tambahBuku;
 */

// TODO: tulis kode kamu di sini
