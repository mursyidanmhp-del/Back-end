/**
 * TUGAS 3 — SEED: ISI DATA CONTOH
 *
 * - require pool dari "../../config/database"
 * - async function seed() yang:
 *   1. DELETE FROM buku dulu (biar aman dijalanin berkali-kali)
 *   2. loop + INSERT 3 buku contoh (judul, penulis, tahun_terbit,
 *      stok) — bebas judul apa aja, pakai placeholder $1/$2/$3/$4,
 *      JANGAN nyambung teks manual
 * - module.exports = seed;
 */

// TODO: tulis kode kamu di sini
