/**
 * TUGAS 1 — config/database.js: CONNECTION POOL
 *
 * Sama persis polanya kayak soal 1 di postgres-migration-dasar:
 * - require("pg"), ambil Pool
 * - new Pool({...}), config dari process.env (PGHOST, PGPORT, PGUSER,
 *   PGPASSWORD, PGDATABASE)
 * - module.exports = pool;
 */

// TODO: tulis kode kamu di sini
