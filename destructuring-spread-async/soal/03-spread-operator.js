/**
 * SOAL 3 — SPREAD OPERATOR
 *
 * Cerita:
 * Mau nambah barang baru ke daftar belanja TANPA mengubah daftar
 * aslinya (nanti dipakai terus di React buat update data tanpa
 * merusak data lama). Caranya pakai spread (...), BUKAN push.
 *
 * Ketentuan:
 * - buat array bernama belanja: ["Roti", "Susu"]
 * - buat array BARU bernama belanjaBaru, isinya = isi belanja + satu
 *   item baru "Telur" di akhir, pakai spread:
 *   const belanjaBaru = [...belanja, "Telur"];
 * - tampilkan belanja.length pakai console.log (harus tetap 2, buktiin
 *   array asli gak berubah), lalu tampilkan belanjaBaru.length pakai
 *   console.log (harus 3)
 * - wajib menggunakan const, "...belanja"
 * - dilarang menggunakan .push(
 * - dilarang menggunakan let, var, function, if, for, while
 *
 * Cara test (jalankan dari folder destructuring-spread-async):
 * npm run test:spread-operator
 */

// TODO: tulis kode kamu di sini

/**
 * Output yang diharapkan:
 * 2
 * 3
 */
